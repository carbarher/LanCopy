# Trust Foundation

This document captures the long-term trust direction:

- explicit peer trust levels
- per-peer permissions
- temporary grants for risky actions
- fingerprint-change alerts
- a single policy matrix used by UI and server

The trust model should stay conservative by default and only unlock risky
operations when the user has opted in explicitly.

